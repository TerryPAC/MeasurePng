// Constants
export const IMAGE_BORDER_OFFSET = 10; // Px offset from image edges to start transparency detection
export const CORNER_DETECTION_RADIUS = 15; // Px radius to detect mouse hover over a corner
export const EDGE_DETECTION_TOLERANCE = 10; // Px distance to detect mouse hover over an edge
export const CHECKERBOARD_TILE_SIZE = 5; // Px size for the canvas background checkerboard tiles
export const MIN_AREA_PIXELS = 100; // Minimum pixel count for a transparent area
export const MIN_AREA_WIDTH = 50; // Minimum pixel width for a transparent area
